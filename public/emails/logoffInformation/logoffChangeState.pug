doctype html
html
    head
        meta(charset='utf-8')
        style(type="text/css") !{compiledStyle}

    body
        div(class="content")
            div(class="greeting")
                h1 Hallo #{contact.firstname}
            div(class="info")
                p Deine Abmeldung von #{`00${logoff.from.getDate()}`.slice(-2)}.#{`00${logoff.from.getMonth() + 1}`.slice(-2)}.#{logoff.from.getFullYear()} bis #{`00${logoff.until.getDate()}`.slice(-2)}.#{`00${logoff.until.getMonth() + 1}`.slice(-2)}.#{logoff.until.getFullYear()} wurde #{(logoff.state === 'approved') ? 'Akezeptiert' : 'Abgelehnt'}.
                if logoff.remarks
                    p Es wurde folgende Bemerkung erfasst:
                    p #{logoff.remarks}
                br
                br
                p Du kannst deine Abmeldung (#[a(href=`${apiEndpoint}/webapp/draft/logoffs/${logoff.id}`) hier]) anschauen.

            br
            br
            div(class="logo")
                img(src=`${apiEndpoint}/logo.png`, alt="VKAZU-logo")