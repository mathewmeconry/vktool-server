doctype html
html
    head
        meta(charset='utf-8')
        block header

body(style={padding: 0, 'font-size': '11pt', 'font-family': 'Calibri, Open Sans, sans-serif'})
    div(style={padding: '0 18mm'})
        block content